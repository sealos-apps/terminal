apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "terminal.fullname" . }}-test-health"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "terminal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:8.10.1
      command:
        - curl
        - --fail
        - --silent
        - --show-error
        - --insecure
        - "https://{{ include "terminal.fullname" . }}-controller-manager-metrics-service.{{ .Release.Namespace }}.svc.cluster.local:8443/metrics"
