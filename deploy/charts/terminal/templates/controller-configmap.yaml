apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "terminal.fullname" . }}-manager-config
  namespace: {{ .Release.Namespace }}
  labels:
    control-plane: controller-manager
    {{- include "terminal.labels" . | nindent 4 }}
data:
  controller_manager_config.yaml: |
    apiVersion: controller-runtime.sigs.k8s.io/v1alpha1
    kind: ControllerManagerConfig
    health:
      healthProbeBindAddress: :8081
    metrics:
      bindAddress: 127.0.0.1:8080
    leaderElection:
      leaderElect: true
      resourceName: 50686b4e.sealos.io
  config.yaml: |
    global:
      cloudDomain: {{ .Values.controller.config.cloudDomain | quote }}
      {{- if .Values.controller.config.cloudPort }}
      cloudPort: {{ .Values.controller.config.cloudPort | quote }}
      {{- end }}
      {{- if .Values.controller.config.httpPort }}
      httpPort: {{ .Values.controller.config.httpPort | quote }}
      {{- end }}
      disableHttps: {{ .Values.controller.config.disableHttps }}
    terminalController:
      ingressTLSSecretName: {{ default .Values.controller.config.ingressTLSSecretName .Values.controller.config.certSecretName | quote }}
