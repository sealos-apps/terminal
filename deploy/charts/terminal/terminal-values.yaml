# Default application values for the unified Sealos Terminal deployment.
# Runtime image references stay in values.yaml so the cluster package can pin
# each release image without being overridden by this application file.

cloudDomain: 127.0.0.1.nip.io
cloudPort: ""
httpPort: ""
disableHttps: false
certSecretName: wildcard-cert

frontend:
  replicaCount: 1
  podAnnotations: {}
  podLabels: {}
  resources:
    limits:
      cpu: 2000m
      memory: 2048Mi
    requests:
      cpu: 10m
      memory: 128Mi
  terminalConfig:
    ttydImage: "docker.io/labring4docker/terminal:1.25.1-5"
    keepalived: "30m"
    # Optional compatibility override for an externally deployed bridge. When
    # empty, the unified chart derives the internal bridge's public origin.
    ttyAgentBaseUrl: ""
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: "app"
                  operator: In
                  values:
                    - terminal-frontend
            topologyKey: "kubernetes.io/hostname"

ttyBridge:
  enabled: true
  replicaCount: 1
  podAnnotations: {}
  podLabels: {}
  ingress:
    enabled: true
  config:
    port: 3000
    kubeApiServer: auto
    wsMaxPayloadBytes: 1048576
    wsHeartbeatIntervalMs: 30000
    wsAuthTimeoutMs: 10000
    wsMaxKubeconfigBytes: 262144
  resources:
    limits:
      cpu: 2000m
      memory: 2048Mi
    requests:
      cpu: 10m
      memory: 128Mi
  livenessProbe:
    httpGet:
      path: /healthz
      port: http
    initialDelaySeconds: 10
    periodSeconds: 20
    timeoutSeconds: 2
  readinessProbe:
    httpGet:
      path: /healthz
      port: http
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 2
  startupProbe:
    httpGet:
      path: /healthz
      port: http
    periodSeconds: 5
    timeoutSeconds: 2
    failureThreshold: 30
  nodeSelector: {}
  tolerations: []
  affinity: {}

controller:
  replicaCount: 1
  podAnnotations: {}
  podLabels: {}
  resources:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 10m
      memory: 64Mi
  livenessProbe:
    httpGet:
      path: /healthz
      port: health
    initialDelaySeconds: 15
    periodSeconds: 20
    timeoutSeconds: 2
  readinessProbe:
    httpGet:
      path: /readyz
      port: health
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 2
  startupProbe:
    httpGet:
      path: /healthz
      port: health
    periodSeconds: 5
    timeoutSeconds: 2
    failureThreshold: 30
  volumes: []
  volumeMounts: []
  nodeSelector: {}
  tolerations: []
  affinity: {}

platform:
  tlsRejectUnauthorized: "1"
